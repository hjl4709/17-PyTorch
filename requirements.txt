# 딥러닝의 정석 with 파이토치 - 실습 환경 (macOS Apple Silicon / Python 3.12)
#
# numpy는 1.x로 고정합니다.
#   - gym 0.26.2 가 np.bool8 을 사용 (numpy 2.0 에서 제거됨)
#   - tensorflow 2.16.x 가 numpy<2 를 요구
numpy==1.26.4

# --- 핵심 프레임워크 ---
# NVIDIA GPU(RTX 50 시리즈 등 Blackwell, compute capability 12.0)에서 CUDA로 잡히려면
# CUDA 12.8+ 로 빌드된 wheel이 필요합니다 (cu126 이하는 미지원).
# 아래 --extra-index-url 에서 torch/torchvision/torchaudio 의 +cu130 빌드를 받아옵니다.
--extra-index-url https://download.pytorch.org/whl/cu130
torch==2.13.0+cu130
torchvision==0.28.0+cu130
torchaudio==2.11.0+cu130
torchsummary

# --- 데이터 / 시각화 ---
matplotlib
seaborn
pandas
scikit-learn
scikit-optimize
yfinance

pillow
opencv-python

librosa
soundfile


transformers
timm

onnx==1.17.0
onnxruntime


gym==0.25.2
pygame


tensorflow==2.16.2


jupyterlab
ipykernel
ipywidgets
